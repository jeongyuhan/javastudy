package ex01_user_type;

/*
	클래스 구성
	1. field(필드) 
		- 클래스에 선언한 변수 (값)
	2. method(메소드)
		- 클래스에 정의한 함수 (기능)
*/

public class Person {

	// field(필드) 선언 (변수를 선언한다.)(클래스에 선언하는 변수는 '필드'라고한다)(메소드 안에서 선언된 변수는 일반변수이다.)	
	// Person을 만들 때 가지게 될 값
	// 일반 변수와 다르게 필드는 자동으로 초기화가 된다.(0, false, null 등 초기화를 가진다.
	String neme;		// null
	int age;			// 0
	double height;		// 0.0
	char gender;	   	// '\0' (null 문자)
	boolean isKorean; 	// false	
	
	

	
	
	
	
	
	
	
	
	
}
