package quiz03;

public class Student {

	void set(String name, int kor, int eng, int math) {
		
	}
	
	double getAverage(int kor, int eng, int math) {
		return (kor + eng + math) / 3.0;
	}
	
	String getPass(double getAverage) {
		return getAverage >= 60 ? "합격" : "불합격";
	}
	
	
}
